# PYAE Revision 2 test fixture

This directory preserves the exact PYAE Revision 2 source and compiled runtime
that immediately preceded the Revision 3 learning-content integrity pass.

The source, compiled runtime, and matching resource-research record are test-only
evidence for revision reconciliation and update-safety checks. They must never be
imported by the learner catalog, treated as an active production curriculum, or
published by the curriculum build pipeline.
